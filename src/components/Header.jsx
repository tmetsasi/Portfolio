import styled from "styled-components";

const StyledHeader = styled.div`
  background-color: grey;
  width: 100%;
  height: 3rem;
`;

export const Header = () => {
  return (
    <div>
      <StyledHeader />
    </div>
  );
};
